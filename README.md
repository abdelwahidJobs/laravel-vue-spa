## ✨ Starter Kit Features
- **Artisan Scaffold:** Run `php artisan spa:install` to automate 2 hours of setup in 2 seconds.
- **Pre-configured Sanctum:** Ready for SPA cookie-based authentication (the secure way).
- **Vue 2 + Router:** Includes a pre-built Router, Login view, and App entry point.
- **Product Module (Optional):** Add `--with-product` to generate a full CRUD with:
    - Automatic Slug generation (Slugify).
    - Grid-refilling pagination (no empty gaps after deletion).
    - PHP 8.2+ optimized code.